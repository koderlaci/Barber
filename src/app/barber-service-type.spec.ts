import { BarberServiceType } from './barber-service-type';

describe('BarberServiceType', () => {
  it('should create an instance', () => {
    expect(new BarberServiceType()).toBeTruthy();
  });
});
